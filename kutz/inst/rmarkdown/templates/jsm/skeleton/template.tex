%%%%%%%%&latex   % causes the production of a .dvi file only and thus remove it
\documentclass[11pt]{asaproc}
%% packages are loaded here to start
\usepackage{graphicx}
\usepackage[hidelinks]{hyperref}
\urlstyle{same}
\usepackage{times}

%% the three-line code below adds code highlighting to our template for R codes
$if(highlighting-macros)$
$highlighting-macros$
$endif$

%% the following lines enable the .csl referencing system for our template
$if(csl-refs)$
\newlength{\cslhangindent}
\setlength{\cslhangindent}{1.5em}
\newlength{\csllabelwidth}
\setlength{\csllabelwidth}{3em}
\newlength{\cslentryspacingunit} % times entry-spacing
\setlength{\cslentryspacingunit}{\parskip}
\newenvironment{CSLReferences}[2] % #1 hanging-ident, #2 entry spacing
 {% don't indent paragraphs
  \setlength{\parindent}{0pt}
  % turn on hanging indent if param 1 is 1
  \ifodd #1
  \let\oldpar\par
  \def\par{\hangindent=\cslhangindent\oldpar}
  \fi
  % set entry spacing
  \setlength{\parskip}{#2\cslentryspacingunit}
 }%
 {}
\usepackage{calc}
\newcommand{\CSLBlock}[1]{#1\hfill\break}
\newcommand{\CSLLeftMargin}[1]{\parbox[t]{\csllabelwidth}{#1}}
\newcommand{\CSLRightInline}[1]{\parbox[t]{\linewidth - \csllabelwidth}{#1}\break}
\newcommand{\CSLIndent}[1]{\hspace{\cslhangindent}#1}
$endif$
%\usepackage{mathtime}

%% title things begin here!
\title{$title$}

%\author{$author-one$ $author-two$}
\author{$author-one$\thanks{$institution-one$} \and  $author-two$\thanks{$institution-two$} %\and Fourth Author\thanks{Fourth author's affiliation,  1100 E. 5th Street, Niles, MI 20724} \and Fifth Author\thanks{Fifth author's affiliation,  1100 E. 5th Street, Niles, MI 20724}
}

\begin{document}


\maketitle

\begin{abstract}
$abstract$
\begin{keywords}
$keywords$
\end{keywords}
\end{abstract}


$body$

\end{document}


